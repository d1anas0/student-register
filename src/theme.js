import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#9FBF9B",
    },
    secondary: {
      main: "#199059",
    },
    text: {
      primary: "#0E1B0E",
    },
    background: {
      default: "#EFEDE6",
    },
  },
});

export default theme;
